import storySnaps from 'test/utils/storySnaps'
import * as stories from './Image.stories'

describe('app/components/atoms/Image', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
